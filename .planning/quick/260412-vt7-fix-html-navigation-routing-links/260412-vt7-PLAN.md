---
must_haves:
  - truths:
      - All navigation to Patient Mode uses `/`
      - All navigation to Doctor Mode uses `/doctor-mode`
      - The strings "doctor.html" and "index.html" do not exist as navigation targets in `static/index.html` or `static/doctor.html`
  - key_links:
      - `static/index.html`
      - `static/doctor.html`
---

# Quick Task 260412-vt7: Fix HTML Navigation Routing Links

## Task 1: Complete HTML Route Fixes
- **files**: 
  - `static/index.html`
  - `static/doctor.html`
- **action**: Open both HTML files. Search for any residual instances of `href="doctor.html"`, `href="index.html"`, `window.location.href='doctor.html'`, or `window.location.href='index.html'`. If any are found, replace them with the FastAPI routes (`/doctor-mode` and `/` respectively). If they are already replaced, document this finding in the summary.
- **verify**: Run `grep` to ensure `"doctor.html"` and `"index.html"` do not exist in the files.
- **done**: The files are fully migrated to FastAPI routed logic.
