---
must_haves:
  - Updates `PATIENT_SYSTEM_PROMPT_TEMPLATE` to start with strict persona lock instructions.
  - Ensures `chat` endpoint correctly constructs `messages` from `[system] + history_dicts` without appending `request.message`.
---
# Quick Task Plan: 260415-o2b-fix-rag-message-construction-and-persona

## Task 1
**Action:** Update `PATIENT_SYSTEM_PROMPT_TEMPLATE` in `fastapi_app.py` to add the requested TheraMind persona lock at the very top of the multi-line string.
**Files:** `fastapi_app.py`
**Verify:** Check that `PATIENT_SYSTEM_PROMPT_TEMPLATE` starts exactly with: "You are TheraMind, a highly advanced clinical intake agent. You MUST remain in character at all times. UNDER NO CIRCUMSTANCES should you refer to yourself as an AI, a language model, or use generic safety disclaimers. If a patient requests dangerous treatments, deny them firmly but empathetically AS the TheraMind agent, using the Clinical Guidelines below."
**Done:** No

## Task 2
**Action:** Verify and preserve the message construction logic in `chat` endpoint of `fastapi_app.py` so it concatenates `system_prompt` and `history_dicts` strictly without manually appending `request.message`.
**Files:** `fastapi_app.py`
**Verify:** Code in `chat` should just do `messages = [{"role": "system", "content": system_prompt}] + history_dicts`.
**Done:** No
